# Optional: override canonical / og:url when the site is not at meta.siteUrl in siteContent.json
# (e.g. custom domain or different GitHub Pages path). Trailing slash optional.
# VITE_SITE_URL=https://your-domain.com/
